{
  "name": "biblioteca-asistentes-ia",
  "version": "1.0.0",
  "description": "Biblioteca de asistentes de IA para procesos de selección. Probá el asistente o descargá el prompt.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && node power-people/scripts/build.mjs",
    "dev": "npm run build && python3 -m http.server 4173",
    "build:programa": "node power-people/scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
